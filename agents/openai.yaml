interface:
  display_name: "Crop Picture"
  short_description: "Crop blank margins from raster images and SVG files."
  default_prompt: "Use $crop-picture to crop blank margins from an image folder."
policy:
  allow_implicit_invocation: true
